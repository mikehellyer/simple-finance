# Simple Finance has no third-party runtime dependencies - it only uses the
# Python standard library (tkinter, sqlite3, urllib). This file exists so the
# packaging/CI steps have a stable place to pin one if that ever changes.
