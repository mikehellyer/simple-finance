# certifi provides a known-good CA certificate bundle for the update
# checker's HTTPS calls. Needed because some PyInstaller build environments
# (e.g. actions/setup-python on macOS) don't leave the frozen app with a
# usable default CA bundle, which otherwise fails HTTPS silently.
certifi==2026.7.22
